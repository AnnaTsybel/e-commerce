import Auth from '@components/Auth';
import { Login } from '@components/Auth/Login';

const LoginPage = () => {
    return (
        <Auth >
            <Login />
        </Auth>
    );
};

export default LoginPage;